version: "3.9"
services:
  db:
    image: mariadb:11
    restart: unless-stopped
    environment:
      MARIADB_ROOT_PASSWORD: rootpass
      MARIADB_DATABASE: muma_importer
      MARIADB_USER: muma_importer
      MARIADB_PASSWORD: muma_importer
    ports:
      - "40000:3306"
    volumes:
      - db_data:/var/lib/mysql

  phpmyadmin:
    image: phpmyadmin:latest
    restart: unless-stopped
    environment:
      PMA_HOST: db
      PMA_USER: muma_importer
      PMA_PASSWORD: muma_importer
    ports:
      - "8080:80"
    depends_on:
      - db

  importer:
    build: .
    depends_on:
      - db
    environment:
      DB_HOST: db
      DB_PORT: 3306
      DB_USER: muma_importer
      DB_PASS: muma_importer
      DB_DB: muma_importer
    ports:
      - "${PORT:-8001}:${PORT:-8001}"
    volumes:
      - .:/app
volumes:
  db_data:
